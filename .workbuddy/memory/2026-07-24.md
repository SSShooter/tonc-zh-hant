# 2026-07-24

## 任务：Tonc（GBA 编程教程 mdBook）全文英译中

将 `content/` 下 34 篇英文教程 + `SUMMARY.md` 目录**就地**翻译为简体中文。README.md 与 maintainer/PORTING_TO_MARKDOWN.md 按用户选择（"仅 content/ 教程"）保留英文。

### 执行方式
- 派发 10 个并行 general-purpose 子代理，按文件大小均衡分批（大文件 mode7ex 3122 行 / asm 2273 / tte 2207 / sndsqr 1810 / matrix 1831 各给单独代理）。
- 每个代理统一规则 + 术语表（GBA、背景、精灵、图块、寄存器、DMA、VBlank、定点数、查找表等）。
- 关键保真规则：代码块、`REG_*`、函数/宏名、`@fig/@tbl/@eq` 引用与 `{#id}` 锚点、MathML/LaTeX、HTML 结构、mdBook `:::` 指令、`<!-- toc -->`、`{{ #include }}` 一律逐字保留；仅译散文，标题序号保留。

### 校验
- 对比 git HEAD：34 文件的代码围栏数、`@fig/@tbl/@eq` 引用数、`{#...}` 锚点数、MathML 数**完全一致**，无破损。
- 补译巡检：修复 regobj/regbg/interrupts/bitmaps/gfx 中约 25 处漏译的图注、提示框标题、寄存器描述表单元格。
- 最终扫描：残留英文均为代码/公式/寄存器标签/注释，无未译散文。

### 备注
- 英文原文在 git 历史可恢复（用户已知）。
- 此"mdBook 英文→中文，保留全部技术语法"的工作流可复用。
